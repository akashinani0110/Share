import plotly.graph_objects as go

# Create a Plotly figure
fig = go.Figure(data=go.Bar(x=["A", "B", "C"], y=[10, 20, 30]))

# Save the figure to an image file
fig.write_image("plotly_plot.png")
print("Plot saved as 'plotly_plot.png'")